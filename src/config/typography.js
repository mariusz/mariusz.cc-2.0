import measure from './measure'

// All typography settings
const theme = {
  typography: {
    font: {
      family: 'Proxima Nova',
      weight: 300,
      sizes: {
        p: `${2 * measure}px`,
        h1: `${6 * measure}px`
      }
    }
  }
};

export default theme;
